# MongoDB connection string. Leave empty to run in DEMO MODE (in-memory store, no persistence).
MONGODB_URI=

# Port the Express + Socket.io server listens on.
PORT=4000

# Origin allowed by CORS / Socket.io. Set to your client URL in production.
CLIENT_ORIGIN=http://localhost:5173
